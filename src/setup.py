from setuptools import setup
from os import path

here = path.abspath( path.dirname( __file__ ) )

setup(
    name='stt-eval',
    version='1.0.0',
    description='Python Flask app for evaluating Speech to Text results',
    license='Apache-2.0'
)
